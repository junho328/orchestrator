defaults:
  - base
  - _self_ 

model_log_name: qwen1bi
model_name_or_path: Qwen/Qwen2.5-1.5B-Instruct

model_args:
  model_revision: main
  torch_dtype: bfloat16
  attn_implementation: flash_attention_2

tokenizer:
  model_revision: ${model_args.model_revision}

